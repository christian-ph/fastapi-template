[POSTGRESQL]
# Basic connection configuration
HOST=localhost
PORT=5432
DB_NAME=db_conf
DB_USER=user_conf
DB_PASSWORD=postgres

# Connection options
POOL_SIZE=5
MAX_OVERFLOW=10
POOL_TIMEOUT=30
POOL_RECYCLE=3600

# Connection options
DB_SCHEMA=schema_conf
SSL_MODE=prefer
CONNECT_TIMEOUT=10
